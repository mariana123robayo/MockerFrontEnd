// Se exporta la constante 'environment' para centralizar la configuración del entorno de la aplicación.
export const environment = {
  // URL base de la API, utilizada para realizar peticiones HTTP. En este caso, se apunta a un servidor local.
  URL: 'http://localhost:8090/api/v1',
};
